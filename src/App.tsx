import './App.css';
import StepperForm from './Steeper';
import { ThemeProvider } from "@mui/material/styles";
// import theme from "./assets";

const App = () => {
  return (
    // <ThemeProvider theme={theme}>
      <StepperForm />
    // </ThemeProvider>
  );
}

export default App;
